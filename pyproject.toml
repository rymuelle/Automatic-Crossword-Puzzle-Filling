[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "crosslift"  # This is what you will 'import'
version = "0.1.0"
dependencies = [
]

[tool.setuptools.packages.find]
where = ["src"] 


[project.scripts]
crosslift-create = "crosslift.create_puzzle:main"